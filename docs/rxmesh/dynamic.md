# **Overview**

*This section is under construction.*

`RXMeshDynamic` extends `RXMeshStatic` with support for topology-changing operations—edge flips, splits, collapses, and other local modifications to the mesh connectivity. It is used for applications such as remeshing, decimation, and surface tracking.

